import 'dart:convert';
import 'package:http/http.dart' as http;
import '../../config/api_config.dart';

class MovieDetailsService {
  // Future<MovieDetailsModel> getMovieDetails(String movieId) async {
  //   final response = await http.get(Uri.parse('$BASE_URL/movies/$movieId'));

  //   if (response.statusCode == 200) {
  //     final jsonData = jsonDecode(response.body);
  //     return MovieDetailsModel.fromJson(jsonData);
  //   } else {
  //     throw Exception('Không lấy được chi tiết phim');
  //   }
  // }
}
